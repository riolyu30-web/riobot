---
name: "toutiao-hot"
description: "当用户需要了解最新热点、榜单或新闻时调用。"
---

# 头条最新热点分析技能

## 功能描述
工作流包括获取榜单、提取新上榜及增量最大的新闻，并逐个搜索详情进行总结发给用户。

## 工作流程 (Workflow)

当触发此技能时，请严格按照以下步骤执行，并且在处理新闻详情时**务必总结一个就发送一个**给用户：

### 1. 获取最新热点榜单
分析用户需要了解的新闻数量，默认值为 10。
调用工具获取当前的头条榜单数据：
```bash
python {homespace}\\tools\\toutiaocli\\cli.py toutiao -r {新闻数量}
```
返回 JSON 格式的头条榜单数据（包含新闻关键词、热度、增量等信息）

### 2. 分析榜单数据并筛选重点
在获取到的 JSON 榜单数据中，分析并提取出以下两类重点新闻词条：
1. **新上榜** 的新闻。
2. **热度增量最大** 的新闻（根据 JSON 对象中的“增量”属性进行判断和排序）。

### 3. 查询详情并逐个总结发送
针对筛选出的重点新闻词条，**必须逐个处理，总结一个发一个**：
1. **查询内容**：调用 `search` 命令查询该关键词的具体新闻内容（注意：对应的实际命令是 `search`）：
```bash
python {homespace}\\tools\\toutiaocli\\cli.py search -k "新闻关键词"
```
2. **总结信息**：阅读搜索返回的数据（包括头条新闻、相关新闻、热门点评等），提炼出事件的核心内容、起因和大众观点。
3. **发送消息**：将该条新闻的总结通过 message 工具发送给用户。
4. **循环执行**：一条新闻发送完毕后，再继续处理下一个关键词，直到所有筛选出的重点新闻都总结汇报完毕。
